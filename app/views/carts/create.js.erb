$('#basket').html("<%= j render partial: 'carts/basket' %>")

$('a.cart.button').toggleClass('primary positive').toggleClass('loading disabled')
    .children('i').toggleClass('shop checkmark').next().text('In Cart');
